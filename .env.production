# ======================================
# EvidLY — PRODUCTION Environment
# ======================================
# Vite loads this file automatically when building with `vite build`
# in production mode (the default for `npx vercel --prod`).
#
# NOTE: Vercel environment variables OVERRIDE these values.
# Set VITE_SUPABASE_URL, VITE_SUPABASE_ANON_KEY, and VITE_APP_ENV
# in Vercel project settings for Production deployments.

VITE_APP_ENV=production
