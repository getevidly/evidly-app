// Centralized config for external URLs and settings
// Change these in one place instead of hunting through components

export const CALENDLY_URL = 'https://calendly.com/evidly/demo';
export const SUPABASE_URL = import.meta.env.VITE_SUPABASE_URL || '';
export const APP_NAME = 'EvidLY';
